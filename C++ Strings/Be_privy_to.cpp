#include <iostream>
#include <string>
#include <cstring>
using namespace std;

int main() {
	string belated = "prevaricate";

	string at_large[] = { "Society at large will benefit from this.",
		"This group is not representative of the population at large." };

	cout << at_large[0];

	return 0;
}